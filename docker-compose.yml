version: '3'

services:

  mysql:
    image: mysql:5
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wordpress
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: password

  wordpress:
    build: ./scripts/docker/wordpress
    depends_on:
      - mysql
    ports:
      - "8810:80"
      - "8811:443"
    volumes:
      - wp_data:/var/www/html/wp-content
      - .:/var/www/html/wp-content/plugins/put-me-anywhere-block
    restart: always
    environment:
      WORDPRESS_DEBUG: 0
      WORDPRESS_DB_USER: wordpress
      WORDPRESS_DB_PASSWORD: password

  mailhog:
    image: mailhog/mailhog
    ports:
      - "1025:1025"
      - "8025:8025"

volumes:
  db_data: {}
  wp_data: {}
